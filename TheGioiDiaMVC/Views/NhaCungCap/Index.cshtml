@model List<TheGioiDiaMVC.ViewModels.NhaCungCapVM>

@{
    ViewData["Title"] = "Danh sách Nhà Cung Cấp";
}
<<<<<<< HEAD
<!-- Single Page Header start -->
<div class="container-fluid page-header py-5">
    <h1 class="text-center text-white display-6">CD LOOP – Rao Bán Ngay!</h1>
</div>

<h2 class="text-center mt-3">Danh Sách Nhà Cung Cấp</h2>

<div class="row mt-4">
    @foreach (var ncc in Model)
    {
        <div class="col-md-3 text-center mb-4">
            <div class="card p-3 shadow">
                <img src="~/Hinh/NhaCC/@ncc.Logo" class="rounded" alt="@ncc.TenNCC" style="width: 220px; height: 220px; object-fit: cover; margin-right: 20px; border-radius: 10px;">

                <h5 class="mt-3">@ncc.TenNCC</h5>
            </div>
        </div>
    }
=======

<!-- Header -->
<div class="container-fluid page-header py-5 bg-dark text-center">
    <h1 class="text-white display-6 fw-bold">CD LOOP – Rao Bán Ngay!</h1>
</div>

<!-- Tiêu đề -->
<h2 class="text-center mt-4 text-success fw-bold">Danh Sách Đối tác CD LOOP</h2>

<!-- Danh sách nhà cung cấp -->
<div class="container mt-4">
    <div class="row justify-content-center">
        @foreach (var ncc in Model)
        {
            <div class="col-md-4 col-sm-6 text-center mb-4">
                <div class="card shadow-lg p-4 border-0">
                    <img src="~/Hinh/NhaCC/@ncc.Logo"
                         class="img-fluid rounded"
                         alt="@ncc.TenNCC"
                         style="width: 100%; height: 300px; object-fit: cover; border-radius: 15px;">

                    <h4 class="mt-3 fw-bold text-primary">@ncc.TenNCC</h4>
                </div>
            </div>
        }
    </div>
>>>>>>> first commit
</div>
